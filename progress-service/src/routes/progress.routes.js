const router=require('express').Router(); const c=require('../controllers/progress.controller'); const {authenticate}=require('../middlewares/auth.middleware'); router.post('/enrollments',authenticate,c.enroll); router.get('/me',authenticate,c.mine); router.put('/lessons',authenticate,c.lesson); router.post('/quiz-attempts',authenticate,c.quiz); router.get('/quiz-attempts',authenticate,c.quizList); module.exports=router;
