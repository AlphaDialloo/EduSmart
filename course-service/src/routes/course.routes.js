const router=require('express').Router(); const c=require('../controllers/course.controller'); const {authenticate,authorize}=require('../middlewares/auth.middleware');
router.get('/',c.list); router.get('/:id',c.getOne); router.post('/',authenticate,authorize('INSTRUCTOR','ADMIN'),c.create); router.post('/:id/modules',authenticate,authorize('INSTRUCTOR','ADMIN'),c.addModule); router.post('/:courseId/modules/:moduleId/resources',authenticate,authorize('INSTRUCTOR','ADMIN'),c.addResource); router.patch('/:id/publish',authenticate,authorize('INSTRUCTOR','ADMIN'),c.publish); module.exports=router;
