const router=require('express').Router(); const c=require('../controllers/interaction.controller'); const {authenticate}=require('../middlewares/auth.middleware'); router.post('/posts',authenticate,c.post); router.get('/courses/:courseId/posts',c.posts); router.post('/posts/:postId/comments',authenticate,c.comment); router.get('/posts/:postId/comments',c.comments); module.exports=router;
