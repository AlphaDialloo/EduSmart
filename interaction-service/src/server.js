require('dotenv').config(); const express=require('express'),cors=require('cors'),helmet=require('helmet'),morgan=require('morgan'),connect=require('./config/mongo'); const routes=require('./routes/interaction.routes'); const app=express(); app.use(cors());app.use(helmet());app.use(morgan('dev'));app.use(express.json());app.get('/health',(_,res)=>res.json({service:'interaction-service',status:'OK'}));app.use('/interactions',routes);connect().then(()=>app.listen(process.env.PORT||4006,()=>console.log('Interaction Service lancé')));
