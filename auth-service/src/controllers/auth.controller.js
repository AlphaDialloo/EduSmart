const bcrypt=require('bcryptjs'), jwt=require('jsonwebtoken'), pool=require('../config/db');
const sign=u=>jwt.sign({id:u.id,email:u.email,role:u.role},process.env.JWT_SECRET||'edusmart_secret_key',{expiresIn:'1d'});
exports.register=async(req,res)=>{const client=await pool.connect();try{const{firstName,lastName,email,password,role='STUDENT'}=req.body;if(!firstName||!lastName||!email||!password)return res.status(400).json({message:'Champs obligatoires manquants'});await client.query('BEGIN');const exists=await client.query('SELECT id FROM auth_service.users WHERE email=$1',[email]);if(exists.rows.length){await client.query('ROLLBACK');return res.status(409).json({message:'Email déjà utilisé'});}const hash=await bcrypt.hash(password,10);const u=await client.query('INSERT INTO auth_service.users(first_name,last_name,email,password_hash) VALUES($1,$2,$3,$4) RETURNING id,first_name,last_name,email',[firstName,lastName,email,hash]);const r=await client.query('SELECT id,name FROM auth_service.roles WHERE name=$1',[role]);if(!r.rows.length){await client.query('ROLLBACK');return res.status(400).json({message:'Rôle invalide'});}await client.query('INSERT INTO auth_service.user_roles(user_id,role_id) VALUES($1,$2)',[u.rows[0].id,r.rows[0].id]);if(role==='STUDENT')await client.query('INSERT INTO user_service.student_profiles(user_id) VALUES($1)',[u.rows[0].id]);await client.query('COMMIT');res.status(201).json({message:'Utilisateur créé',user:u.rows[0],role,token:sign({id:u.rows[0].id,email,role})});}catch(e){await client.query('ROLLBACK');res.status(500).json({message:'Erreur serveur',error:e.message});}finally{client.release();}};
exports.login=async(req,res)=>{try{const{email,password}=req.body;const q=await pool.query(`SELECT u.id,u.first_name,u.last_name,u.email,u.password_hash,r.name role FROM auth_service.users u JOIN auth_service.user_roles ur ON ur.user_id=u.id JOIN auth_service.roles r ON r.id=ur.role_id WHERE u.email=$1`,[email]);if(!q.rows.length)return res.status(401).json({message:'Identifiants incorrects'});const u=q.rows[0];if(!await bcrypt.compare(password,u.password_hash))return res.status(401).json({message:'Identifiants incorrects'});res.json({message:'Connexion réussie',token:sign(u),user:{id:u.id,firstName:u.first_name,lastName:u.last_name,email:u.email,role:u.role}});}catch(e){res.status(500).json({message:'Erreur serveur',error:e.message});}};
exports.me=(req,res)=>res.json({user:req.user});
